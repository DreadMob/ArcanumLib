using Xunit;

namespace ArcanumLib.Tests.Unit;

// EffectState tests now use the ArcanumServices collection to share the same runtime.
// This file is kept for backward compatibility but no longer defines a separate collection.
