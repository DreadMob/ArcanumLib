# ModAssetRegistry

Builds validated, keyed, source-tracked registries from `ModAssetLoader` output.
Useful for mods that need to collect and merge definitions from multiple packs
and then look them up by code.

See the source for the current API:
- `src/Assets/ModAssetRegistry.cs`
